import { EntityFormUiComponent } from "./entity-form.component";

describe("EntityFormUiComponent", () => {
  it("should be defined", () => {
    expect(EntityFormUiComponent).toBeTruthy();
  });
});
